<%= form_for [topic, comment] do |f| %>
	<%= render 'shared/error_messages', object: comment %>

  <div class="field">
    <%= f.label :description, 'Comment' %><br />
    <%= f.text_area :description, placeholder: "What's up..." %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
